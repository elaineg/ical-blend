# Dana — Round 1
- Advocacy: 9/10
- Clarity: Yes
- Value: Yes
- Found-preview-cold: Yes
- Top blocker: "Load a sample feed" pastes URLs but does NOT auto-preview — I still had to tap "Preview merged calendar" to see anything populated. One extra tap between me and the payoff.
- Prior concern addressed: Yes

## Walkthrough (incl. mobile 375px notes)
Cold load at 375px. Headline + "Paste 2–5 calendar links. Preview & test them, then get one feed. No account." + "LOAD A SAMPLE FEED" all visible inside the first scroll. No layout break, no horizontal scroll anywhere I went.
- Tapped LOAD A SAMPLE FEED cold: it filled two source boxes (UK bank holidays + Chelsea fixtures) but the result area stayed empty. Expected an instant populated merge; got pasted URLs. Had to tap PREVIEW.
- PREVIEW MERGED CALENDAR: ~few sec fetch, then STATUS block: "✓ [Holidays] — 83 events fetched / ✓ [Personal] — 325 events fetched", then "Fetched 408 events → kept 408 after filters & mask", then "PREVIEW · NEXT 15" — a clean chronological list with [source] labels per event. This is exactly the trust signal I wanted.
- Bad URL test (swapped one source to a fake .ics): "✗ [Holidays] — fetch failed" in red + "1 source failed — its events are not included." Honest, not a silent swallow.
- Keyword filter "Christmas" on the fixtures feed: count dropped to "kept 0" with "0 kept — your filters removed every event. Loosen a keyword or mask above." Counts move with my filters = I trust them.

## Answers
1. CLARITY: Yes. In ~30s on my phone I'd tell a friend: "merge a few calendar feeds into one link you subscribe to once, with keyword filters and a privacy mask, no signup." The "no account" + sample-feed combo sold it. Minor: the headline ("work, personal, shared calendars") reads consumer, not marketer — I had to infer my webinar/HubSpot case would work; it's not pitched at me.
2. VALUE: Yes. Today I manually juggle a Luma/event ICS, GA4-driven Notion reminders, and would kill to fold a HubSpot campaign-milestone feed into one phone-subscribable calendar. There's no free no-signup tool that does keyword-filtered ICS merge + a busy-only mask — this beats my copy-paste-into-Google-Calendar habit. The "kept Y after filters" honesty is what makes me trust subscribing.
3. ADVOCACY: 9/10. I'd screenshot the STATUS + counts block into the team channel — that's the screenshot-worthy moment. Not a 10 only because the sample loads URLs instead of an instant populated merge (one extra tap from cold to "wow"), and the headline isn't aimed at marketers so a peer skimming might not see themselves. Fix the auto-preview-on-sample and it's a 10.
