```json
{"tester":5,"name":"Dana","clarity":"Yes","value":"Yes","advocacy":9}
```

# Dana — Demand-gen marketer (medium tech, MacBook) — Round 2

## Prior concerns (round 1) — both ADDRESSED
- **Copy button gave no feedback** → FIXED. Clicked Copy: label flipped to "Copied!" for
  ~2s then reverted to "Copy". Clipboard genuinely held the full feed URL
  (`.../api/feed/LjaVYP5j...`). This was my single biggest blocker; it's the difference
  between me trusting it and clicking twice. Done right.
- **Duplicate events across feeds** → FIXED. Pasted both gov.uk feeds (England+Wales,
  Scotland). Christmas Day, Boxing Day, New Year's Day each appear exactly ONCE now;
  Scotland's St Andrew's Day still shows (correctly unique). "Summer bank holiday" appears
  twice but those are two different dates (Aug 3 + Aug 31), so that's correct, not clutter.

## What ELSE got better (pleasant surprise)
- New headline "Stop checking three calendars" is a sharper hook than "iCal Blend" — sells
  the pain in one line.
- Result now says "Merged 114 events from 2 sources" — a concrete count I trust at a glance.
- "Add to Google Calendar" one-click button + explicit Apple/Outlook subscribe steps. As a
  marketer who lives in Google Cal, that one button removes the only fiddly step.
- Both https Feed URL and webcal:// link present — webcal is my one-tap phone subscribe.

## 1. CLARITY — Yes
One scroll, zero confusion. "Paste 2–5 calendar links. Get one feed. No account." plus the
prefilled source rows = instantly legible.

## 2. VALUE — Yes
What I do today: eyeball webinar cal + HubSpot milestones separately, re-key dates into
Notion. This genuinely collapses that into one subscribable feed. Same real-world asterisk
as round 1: HubSpot/GA4 don't emit ICS, so my two literal stars won't merge — but Google
Cal + my webinar platform do, so it covers most of my world. Not the app's fault.

## Remaining friction (minor, holds it under 10)
- Still no hint on WHERE to find an ICS URL inside common tools (Google "Secret address in
  iCal format", etc.). A one-liner would stop non-technical teammates bouncing.
- That's the only thing between this and a 10. The core flow is now clean.

## Advocacy — 9 (up from 7)
Both things I dinged it for are fixed and verified. I'd screenshot the "Add to Google
Calendar" button + deduped preview for our team channel unprompted. Not a 10 only because
a "where do I get my ICS link" hint would make it shareable with my less-technical peers.
