# Dana — Demand-gen marketer (Round 2)

(a) Advocacy: **8/10**
(b) Value clear in <30s: **Yes**
(c) Biggest remaining blocker: **No way to merge feeds that need auth/refresh-friendly handling, and the preview/value pitch is "personal calendars," not "marketing feeds" — I had to translate it to my use case myself.**

## Prior concern (R1): Options hard to find — ADDRESSED.
The per-feed "Options" toggle now reads **"Options · on"** when a setting is active, sits right under each URL row, and expands a clean gray panel with prefix + "Mask this feed's titles" + "Hide all-day events." I found it in one click. No more hunting. Fixed.

## Fresh take
- Merged the two real holiday feeds, **59 events from 2 sources**, no errors. Set `[US]` prefix on one — showed up correctly as `[US] Independence Day`. Masked Canada — showed as `Busy`, and I confirmed no Canada titles leaked into the preview. Privacy fix holds.
- The privacy note ("encrypted, self-contained, nothing stored server-side") is exactly the line I'd screenshot for my team channel — that's the trust unlock.
- "Your recent blends" with an **editable nickname** field ("Add a nickname (e.g. Client A)") is the recall I needed; I'd label them "Webinars" / "HubSpot milestones."
- Why not 9: the headline + copy is all "work/personal/shared calendars." For my actual job (webinar/event ICS + HubSpot campaign feed → one subscribable phone feed) I had to do the mental translation. One marketer-facing example would push this to a 9. Also the feed URL is a monster string — fine since I just hit Copy, but visually alarming.
- I'd recommend it to my team today for merging public/event feeds. Held back from "bring it up unprompted everywhere" only by the positioning.

```json
{"tester": 0, "round": 2, "clarity": "Yes", "value": "Yes", "advocacy": 8, "topComplaints": ["Positioning is personal-calendar, not marketer/event-feed — I translated the use case myself", "Feed URL is an intimidatingly long string"], "priorConcernsAddressed": "all"}
```
